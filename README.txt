A page which uses a quote database API to randomly pull a quote and display it. 

You can then tweet the returned quote. 

Future updates will allow you to enter a search term to specify quotes on a topic.